spring:
  datasource:
    url: jdbc:h2:mem:edutest_test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: false
  # Disable Flyway in tests — Hibernate create-drop handles schema for H2,
  # and the V1 baseline placeholder is empty (would fail on V2 ALTER otherwise).
  flyway:
    enabled: false

# Disable rate limiting in tests — they fire many requests per user
app:
  rate-limit:
    enabled: false
  h2:
    console:
      enabled: false

# JWT Configuration for tests
app:
  jwtSecret: testSecretKeyForJwtTokenGenerationInTestsWithAtLeast256Bits1234567890
  jwtExpirationMs: 86400000
  refreshTokenExpirationMs: 604800000

# Logging
logging:
  level:
    com.edutest: DEBUG
    org.springframework.security: DEBUG
